#ifndef VECTOR_HPP
#define VECTOR_HPP
#include <memory>
#include <vector>
#include <iostream>

using namespace std;
namespace ft
{
template < class T, class Allocator = std:: allocator<T> class vector
template < typename L >class RandomAccessIterator
	{
		public:
		typedef typename iterator_traints<L* > ::value value;

		private:
			pointer _ptr;
		
		RandomAccessIterator() : _ptr() {}
		public:
			void getvalue()
			{
			return _value;
			}
		}
	}
